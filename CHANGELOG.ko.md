# 변경 이력

[English](CHANGELOG.md) | [한국어](CHANGELOG.ko.md)

## 0.4.1 — 2026-09-22

[업데이트 안내](docs/updates/v0.4.1.ko.md) · [English](docs/updates/v0.4.1.md)

- GitHub 릴리스 초안 조회를 수정했습니다. 태그 조회 API는 게시된 릴리스만 반환하므로, 인증된 목록을 페이지별로 확인하고 일치하는 초안을 숫자 ID로 다시 읽어 업로드 파일을 검증합니다.
- 중복·조회 실패·도중에 바뀐 초안은 게시를 중단합니다. 기존 태그·파일을 보존하며 바이트가 일치할 때만 이어서 게시합니다.
- 0.4.0 기능은 네 CI 환경을 모두 통과했습니다(Windows 286개, 나머지 284개와 Windows 전용 2개 제외). 첫 자동 릴리스가 위 오류로 초안에 남아, 수정한 0.4.1을 게시 버전으로 삼습니다. 기존 0.4.0 태그·초안은 옮기거나 삭제·덮어쓰지 않았습니다.

## 0.4.0 — 2026-09-22

[업데이트·적용 안내](docs/updates/v0.4.0.ko.md) · [English update notes](docs/updates/v0.4.0.md)

- MCP 활성·대기 요청 취소, 응답 가능한 입력 처리, 최대 16개 도구 요청 대기열과 단일 상태 소유자를 도입했습니다. 취소된 요청은 응답하지 않으며 EOF에서 대기 작업과 HTTP 연결을 정리합니다.
- 이름·설명 기반 후보 검색을 명시적으로 선택할 수 있습니다. 후보 제외 범위와 동점을 표시하고, 검색 결과가 없으면 키 조회·과금 전에 중단합니다. 기본 전체 목록 평가와 Jev 검증 조건은 유지합니다. plan/route의 일회성 옵션과 4,096개 합성 스킬의 요청 상한 비교를 추가했습니다.
- 설정 보존형 설치 업데이트, 설치 버전 확인, 반복 가능한 호스트 등록, Codex 환경 키 이름 전달, 기존 모드 보존, `setup --live`와 `--version`을 추가했습니다.
- 영어·한국어 96개 자체 작성 사례, 독립 어휘 기준선 두 개, 실제 후보 검색 유지율, 공통 요청 상한이 있는 Jev 비교 도구를 추가했습니다. 기준선 결과를 공개하되 실제 Jev 정확도로 표시하지 않습니다.
- 공식 MCP SDK 1.30.0의 stdio 왕복과 실제 Codex CLI 0.155.1 / Claude Code 2.1.278의 임시 프로필 등록·연결을 확인했습니다. 모델을 사용한 실제 작업 완수는 아직 검증하지 않았습니다.
- 테스트를 통과한 커밋의 GitHub 릴리스 자동화를 추가했습니다. 한영 버전 자료, wheel·sdist·소스 ZIP·SHA256SUMS를 확인하며 기존 태그·파일을 덮어쓰지 않고 중단된 초안을 복구합니다.
- 검색 메타데이터를 포함해 합성 컨텍스트 계산을 갱신했습니다. 200개는 UTF-8 58,088 → 4,940바이트(91.50% 감소), 5개는 27.28% 증가합니다. 토큰·가격·속도 측정값이 아닙니다.

## 0.3.0 — 2026-09-22

[업데이트 내용과 기존 사용자 적용 방법](docs/updates/v0.3.0.ko.md)

- `jev-skills plan TASK [--context TEXT]`를 추가했습니다. API 키 조회나 모델 호출 없이 요청 수의 상한, 재시도 여유, 제한 초과 여부를 확인합니다. 실제 라우팅도 같은 사전 점검을 먼저 실행합니다.
- 파일마다 `content_digest`를 반환합니다. **호환성 변경:** 0보다 큰 오프셋으로 이어 읽을 때 MCP의 `expected_digest` 또는 CLI의 `--expected-digest`가 필요합니다. 파일이 바뀌면 다른 버전의 페이지가 섞이기 전에 중단합니다.
- 예약된 스킬 이름 `jev-skill-router`를 선택 대상에서 제외했습니다. 안내용 스킬은 디스크에 유지되며, 파일 분석 결과도 재사용합니다.
- 원래 경로의 심볼릭 링크를 확인한 뒤 동일한 폴더를 가리키는 중복 경로를 통합합니다. 읽지 못한 하위 폴더와 잘못된 메타데이터의 원인도 표시합니다.
- 설치 설정 시 지정한 `max_catalog_skills` 제한을 적용합니다.
- 과도하게 중첩된 JSON, 잘못된 유니코드, 비정상 숫자 입력 때문에 MCP 서버가 종료되지 않도록 수정했습니다. 잘못된 Jev 응답은 민감한 원문을 노출하지 않는 오류로 처리합니다.
- 도구 스키마와 파일 버전 정보가 늘어난 만큼 합성 컨텍스트 비교 수치를 다시 계산했습니다. 실제 Jev 정확도·비용·속도 개선을 입증하는 수치는 아닙니다.
- Ubuntu Python 3.11/3.13, macOS Python 3.12, Windows Python 3.12 CI를 통과했습니다. Windows는 201개 전부, 나머지 환경은 각각 199개 통과·Windows 전용 2개 제외입니다. [검증 결과](https://github.com/himomohi/jev-skill-router/actions/runs/35737405992).

## 0.2.1 — 2026-09-22

- 작업별 본문 발췌를 후보 스킬에만 수행합니다. 미리 계산한 문자별 JSON 바이트 정보를 이용해 검증 요청 크기의 상한을 계산합니다.
- Windows NTFS/ReFS의 변경 시각을 이용해 변경 없는 파일을 재사용합니다. 확인이 불가능하면 전체 내용을 다시 읽고, 재분석 지점을 통한 경로 우회는 거부합니다.
- 모델 검증 중 스킬 파일이 바뀌면 본문 반환과 결과 캐싱 전에 거부합니다.
- 평가 보고서 버전 3에서 첫 요청과 반복 요청의 측정 구간을 맞추고, 실패한 반복 요청도 기록합니다. 버전 2와의 비교는 거부합니다.
- CI 작업 버전을 고정하고 모든 플랫폼에서 키가 필요 없는 평가 사전 점검을 실행합니다.

## 0.2.0 — 2026-09-22

- Linux/macOS에서 변경 없는 파일의 분석 결과를 재사용하고, 변경 시 선택 결과 캐시를 무효화합니다.
- 긴 스킬 본문의 시작·끝·관련 중간 구간에서 제한된 검증 근거를 추출합니다.
- 독립적인 Jev 요청을 기본 최대 3개씩 처리합니다. 전체 45초의 시간 예산과 재시도를 포함한 HTTP 요청 한도를 적용합니다.
- 영어·한국어 24개 사례로 선택 결과, 첫 요청·캐시 적중 시간, 요청 수, 사용량을 기록하는 선택적 실제 API 평가 도구를 추가했습니다.
- 합성 바이트 비교와 실제 작업 성능을 구분해 문서화했습니다.

## 0.1.0 — 2026-09-22

최초 소스 공개 버전입니다. 외부 `SKILL.md` 목록, Jev의 후보 순위 판단과 적합도 검증, 읽기 전용 MCP 도구, 메모리 캐시, 참조 파일 읽기, 오프라인 데모, 운영체제 키체인, 클라이언트 연결을 제공합니다.

스킬 이동·복구의 사전 확인, 합성 컨텍스트 비교, 한·영 문서와 소개 영상도 포함합니다. 당시 로컬 테스트 72개와 Linux/macOS/Windows CI를 통과했습니다. 과거 검증과 현재 결과의 구분은 [검증 기록](docs/VALIDATION.md)을 참고하세요.
