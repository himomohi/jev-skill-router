---
name: csv-profile
description: Inspect CSV columns, encoding, missing values and duplicate rows without modifying data. CSV 인코딩 열 결측치 중복 행 읽기 전용 점검.
---

Read the file conservatively, report encoding, column names, row counts, null counts and duplicate counts. Do not rewrite, clean or convert the input.
