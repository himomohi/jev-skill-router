---
name: docker-debug
description: Debug Docker container startup, Dockerfile builds, mounts and network connectivity. Docker 컨테이너 시작 빌드 볼륨 네트워크 오류 해결.
---

Inspect the Dockerfile, container logs, mount configuration and network boundaries, reproduce the failure and apply a focused fix. Do not modify production containers without authorization.
